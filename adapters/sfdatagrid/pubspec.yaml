name: volvoxgrid_sfdatagrid
description: SfDataGrid-compatible VIEW adapter for VolvoxGrid
version: 0.1.0
homepage: https://github.com/ivere27/volvoxgrid
repository: https://github.com/ivere27/volvoxgrid/tree/main/adapters/sfdatagrid

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  volvoxgrid: ^0.2.0
  # Local monorepo override:
  # volvoxgrid:
  #   path: ../../flutter
  syncfusion_flutter_datagrid: ^30.2.7 # Proprietary license — see https://www.syncfusion.com/products/communitylicense

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
