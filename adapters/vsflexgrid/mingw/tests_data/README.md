Data-focused scripts for `run_compare_data.sh`.

- Numbering follows the shared compare table in `grid_compare_test.c`.
- Keep only data-oriented scripts here.
- Default data suite currently runs:
  - `66_datasource_bind.vbs`
  - `67_data_roundtrip_refresh.vbs`
