/* Code generated by protoc-gen-synurang-ffi. DO NOT EDIT. */
/* C Native API Header for volvoxgrid.v1 */

#pragma once

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif


/* ═══════════════════════════════════════════════════════════ */
/* VolvoxGridService — Native C API                            */
/* ═══════════════════════════════════════════════════════════ */


/* Create */
uint8_t* volvox_grid_create(
    int32_t viewport_width,
    int32_t viewport_height,
    float scale,
    const uint8_t* config, int32_t config_len,
    int32_t* out_len
);

/* Destroy */
uint8_t* volvox_grid_destroy(
    int64_t grid_id,
    int32_t* out_len
);

/* Configure */
uint8_t* volvox_grid_configure(
    int64_t grid_id,
    const uint8_t* config, int32_t config_len,
    int32_t* out_len
);

/* GetConfig */
uint8_t* volvox_grid_get_config(
    int64_t grid_id,
    int32_t* out_len
);

/* LoadFontData (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_load_font_data_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* DefineColumns (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_define_columns_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* GetSchema */
uint8_t* volvox_grid_get_schema(
    int64_t grid_id,
    int32_t* out_len
);

/* DefineRows (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_define_rows_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* InsertRows (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_insert_rows_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* RemoveRows */
uint8_t* volvox_grid_remove_rows(
    int64_t grid_id,
    int32_t index,
    int32_t count,
    int32_t* out_len
);

/* MoveColumn */
uint8_t* volvox_grid_move_column(
    int64_t grid_id,
    int32_t col,
    int32_t position,
    int32_t* out_len
);

/* MoveRow */
uint8_t* volvox_grid_move_row(
    int64_t grid_id,
    int32_t row,
    int32_t position,
    int32_t* out_len
);

/* UpdateCells (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_update_cells_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* GetCells */
uint8_t* volvox_grid_get_cells(
    int64_t grid_id,
    int32_t row1,
    int32_t col1,
    int32_t row2,
    int32_t col2,
    int32_t include_style,
    int32_t include_checked,
    int32_t include_typed,
    int32_t include_barcode_status,
    int32_t* out_len
);

/* LoadTable (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_load_table_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* LoadData (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_load_data_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* AppendData (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_append_data_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Clear */
uint8_t* volvox_grid_clear(
    int64_t grid_id,
    int32_t scope,
    int32_t region,
    int32_t* out_len
);

/* Select (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_select_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* GetSelection */
uint8_t* volvox_grid_get_selection(
    int64_t grid_id,
    int32_t* out_len
);

/* ShowCell */
uint8_t* volvox_grid_show_cell(
    int64_t grid_id,
    int32_t row,
    int32_t col,
    int32_t* out_len
);

/* SetTopRow */
uint8_t* volvox_grid_set_top_row(
    int64_t grid_id,
    int32_t row,
    int32_t* out_len
);

/* SetLeftCol */
uint8_t* volvox_grid_set_left_col(
    int64_t grid_id,
    int32_t col,
    int32_t* out_len
);

/* Edit (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_edit_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Sort (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_sort_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Subtotal */
uint8_t* volvox_grid_subtotal(
    int64_t grid_id,
    int32_t aggregate,
    int32_t group_on_col,
    int32_t aggregate_col,
    const uint8_t* caption, int32_t caption_len,
    uint32_t background,
    uint32_t foreground,
    int32_t add_outline,
    const uint8_t* font, int32_t font_len,
    int32_t* out_len
);

/* AutoSize */
uint8_t* volvox_grid_auto_size(
    int64_t grid_id,
    int32_t col_from,
    int32_t col_to,
    int32_t equal,
    int32_t max_width,
    int32_t* out_len
);

/* Outline */
uint8_t* volvox_grid_outline(
    int64_t grid_id,
    int32_t level,
    int32_t* out_len
);

/* GetNode (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_get_node_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Find (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_find_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Aggregate */
uint8_t* volvox_grid_aggregate(
    int64_t grid_id,
    int32_t aggregate,
    int32_t row1,
    int32_t col1,
    int32_t row2,
    int32_t col2,
    int32_t* out_len
);

/* GetMergedRange */
uint8_t* volvox_grid_get_merged_range(
    int64_t grid_id,
    int32_t row,
    int32_t col,
    int32_t* out_len
);

/* MergeCells */
uint8_t* volvox_grid_merge_cells(
    int64_t grid_id,
    const uint8_t* range, int32_t range_len,
    int32_t* out_len
);

/* UnmergeCells */
uint8_t* volvox_grid_unmerge_cells(
    int64_t grid_id,
    const uint8_t* range, int32_t range_len,
    int32_t* out_len
);

/* GetMergedRegions */
uint8_t* volvox_grid_get_merged_regions(
    int64_t grid_id,
    int32_t* out_len
);

/* GetMemoryUsage */
uint8_t* volvox_grid_get_memory_usage(
    int64_t grid_id,
    int32_t* out_len
);

/* Clipboard (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_clipboard_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Export */
uint8_t* volvox_grid_export(
    int64_t grid_id,
    int32_t format,
    int32_t scope,
    int32_t* out_len
);

/* Print (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_grid_print_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Archive */
uint8_t* volvox_grid_archive(
    int64_t grid_id,
    const uint8_t* name, int32_t name_len,
    int32_t action,
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* ResizeViewport */
uint8_t* volvox_grid_resize_viewport(
    int64_t grid_id,
    int32_t width,
    int32_t height,
    int32_t* out_len
);

/* SetRedraw */
uint8_t* volvox_grid_set_redraw(
    int64_t grid_id,
    int32_t enabled,
    int32_t* out_len
);

/* Refresh */
uint8_t* volvox_grid_refresh(
    int64_t grid_id,
    int32_t* out_len
);

/* LoadDemo */
uint8_t* volvox_grid_load_demo(
    int64_t grid_id,
    const uint8_t* demo, int32_t demo_len,
    int32_t* out_len
);

/* GetDemoData */
uint8_t* volvox_grid_get_demo_data(
    const uint8_t* demo, int32_t demo_len,
    int32_t* out_len
);


/* ═══════════════════════════════════════════════════════════ */
/* Memory Management & Error Retrieval                        */
/* ═══════════════════════════════════════════════════════════ */

/* Free memory returned by native API functions.              */
/* The allocation size is recovered from the prepended header. */
void volvox_grid_free(uint8_t* ptr);

/* Returns a pointer to the last error message (UTF-8, not    */
/* null-terminated). Valid until the next FFI call into this   */
/* module. Returns NULL if no error. Writes byte length to    */
/* out_len.                                                   */
const uint8_t* volvox_grid_last_error(int32_t* out_len);


#ifdef __cplusplus
}
#endif

