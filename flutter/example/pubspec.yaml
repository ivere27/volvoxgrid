name: volvoxgrid_example
description: VolvoxGrid example app — sales data grid demo
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  volvoxgrid:
    path: ../
  volvoxgrid_sfdatagrid:
    path: ../../adapters/sfdatagrid
  syncfusion_flutter_datagrid: ^30.2.7 # Proprietary license — see https://www.syncfusion.com/products/communitylicense

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
